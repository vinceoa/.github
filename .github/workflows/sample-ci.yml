name: Sample CI

on: 
  workflow_call:
    inputs:
      input1:
        required: false
        type: string
      input2:
        required: false
        type: string

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello from Octo Organization

      - name: Show ENV
        run: env
